// Wave 1 cleanup — Dosar unificat
// Absoarbe: dovezi + documente generate + exporturi + politici + audit trail
// URL: /dashboard/dosar (înlocuiește /dashboard/reports în nav principal)

import { DosarPageSurface } from "@/components/compliscan/dosar-page"

export default function DosarPage() {
  return <DosarPageSurface />
}
